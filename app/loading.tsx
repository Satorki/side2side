"use client";
import React from "react";
import LoadingBar from "react-top-loading-bar";

const loading = () => {
  return <LoadingBar color="#96656f" progress={100} height={3} />;
};

export default loading;
